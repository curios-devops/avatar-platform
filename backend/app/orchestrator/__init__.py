"""ETAPA A — orquestador conversacional (docs/nueva_arquitectura.md).

Fase 0: `contract` (mensaje compartido Feed/3D).
A0: `llm` + `sentences` + `tts` + `visemes` + `stt` + `session`,
expuesto por WebSocket en `app.api.converse`.
"""
